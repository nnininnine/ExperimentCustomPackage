import XCTest
@testable import ExperimentCustomPackage

final class ExperimentCustomPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ExperimentCustomPackage().text, "Hello, World!")
    }
}
